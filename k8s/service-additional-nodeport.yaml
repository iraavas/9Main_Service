apiVersion: v1
kind: Service
metadata:
  name: additional-service-nodeport
spec:
  type: NodePort
  selector:
    app: additional-service
  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 30082